export const tabsVariant = {
  default: 'default',
  underline: 'underline',
  pills: 'pills',
} as const;

export const tabsTransition = {
  slide: 'slide',
  fade: 'fade',
  scale: 'scale',
  flip: 'flip',
  bounce: 'bounce',
} as const;
